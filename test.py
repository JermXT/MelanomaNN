import nnOutput, impageprocOut, sys
total = len(sys.argv)
def run(total):
    if(total > 1):
        print "too many args"
        return
    const = 0.9
    answer = False
    while(imageprocout)
    impageprocout
